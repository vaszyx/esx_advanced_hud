-- Placeholder for HUD server logic.
-- Use this file to persist player HUD preferences or integrate logging later on.

RegisterNetEvent('rs_hud:saveSettings', function(settings)
  -- Persist settings via oxmysql/kvp/etc. when needed
end)
